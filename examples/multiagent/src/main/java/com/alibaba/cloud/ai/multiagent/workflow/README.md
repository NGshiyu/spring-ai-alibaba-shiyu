# Spring AI Alibaba MultiAgent Workflow ????

??????? Graph ??????? Workflow Agent?
- Graph??????
- ???????????
- 
- ?? AgentCardProvider ???? A2aRemoteAgent ????

## ????

### A2A ???????

1. **?? Agent ??**?`A2AAgentConfig` ?? ReactAgent Bean
2. **A2A Server ????**?Spring Boot ????A2A Server AutoConfiguration ???
    - ?? ReactAgent Bean ?? AgentCard
    - ?? REST API ???`/.well-known/agent.json` ? `/a2a/message`?
3. **Nacos Registry ??**??? `registry.enabled: true` ??
    - ??? AgentCard ??? Nacos A2A ?????
    - ??????? Nacos ??? Agent
4. **Nacos Discovery ??**??? `discovery.enabled: true` ??
    - AgentCardProvider ?? Nacos ?????? Agent
    - ?? A2aRemoteAgent ??????

## ??
- `A2AAgentConfig.java`????? ReactAgent?data_analysis_agent?
- `A2AExample.java`???????????????????????
- `A2AExampleController.java`??? `/api/a2a/demo` HTTP ??
- `application.yml`?**A2A server?Nacos Registry ? Nacos Discovery ??**

## ????

### ??????application.yml?

```yaml
spring:
  ai:
    alibaba:
      a2a:
        nacos:
          server-addr: 127.0.0.1:8848
          username: nacos
          password: nacos
          discovery:
            enabled: true   # ??????????? Agent?
          registry:
            enabled: true   # ??????????? Agent?
        server:
          version: 1.0.0
          card:
            name: data_analysis_agent
            description: ???????????????????
            provider:
              name: Spring AI Alibaba Documentation
              organization: Spring AI Alibaba
```

**??**?
- `registry.enabled: true` - ??????? Agent ??? Nacos
- `discovery.enabled: true` - ??????? AgentCardProvider ???? Agent
- `server.card` - ????? Nacos ? AgentCard ???

## ??

### 1) ????

```bash
export DASHSCOPE_API_KEY=your-api-key
export NACOS_SERVER_ADDR=127.0.0.1:8848
export NACOS_USERNAME=nacos
export NACOS_PASSWORD=nacos
```

???????? Nacos??? Docker ????
```bash
docker run --name nacos -d -p 8848:8848 -e MODE=standalone nacos/nacos-server:latest
```

### 2) ????

```bash
mvn -q -pl examples/documentation -am spring-boot:run
```

### 3) ??????

```bash
curl http://localhost:8080/api/a2a/demo
```

### 4) ???????

**?? AgentCard**?
```bash
curl http://localhost:8080/.well-known/agent.json
```

**Nacos ???**?
- ?? http://localhost:8848/nacos
- ???nacos/nacos?
- ??????? **Agent??** ??
- ??????????? `data_analysis_agent` ?????????

**?? Nacos ???? API ??**?
```bash
# ?? agent-endpoints ????????
curl "http://localhost:8848/nacos/v1/ns/service/list?pageNo=1&pageSize=10&groupName=agent-endpoints&username=nacos&password=nacos"

# ???? Agent ???????????? Nacos ?????????
curl "http://localhost:8848/nacos/v1/ns/instance/list?serviceName=____:data_095analysis_095agent::1.0.0&groupName=agent-endpoints&username=nacos&password=nacos"
```

> **??**?
> - Agent ???? Nacos ??????????????? A2A REST API
> - ???????`____:agent_name::version`??????????????
> - API ?????? Nacos ?????username ? password?

## ????

### ????????

1. **?? ReactAgent Bean**?`A2AAgentConfig.java`??
```java
@Bean(name = "dataAnalysisAgent")
public ReactAgent dataAnalysisAgent(@Qualifier("dashscopeChatModel") ChatModel chatModel) {
    return ReactAgent.builder()
            .name("data_analysis_agent")
            .model(chatModel)
            .description("???????????????????")
            .instruction("?????????????...")
            .outputKey("messages")
            .build();
}
```

2. **?? A2A Server ? Nacos Registry**?`application.yml`??
    - `spring.ai.alibaba.a2a.server.card` - ?? AgentCard
    - `spring.ai.alibaba.a2a.nacos.registry.enabled: true` - ????

3. **??????**?
    - Spring Boot ?? ? A2A Server AutoConfiguration ??? ReactAgent Bean
    - ?? AgentCard ? ??? Nacos A2A Registry
    - ?? REST API ??

### ????????`A2AExample.java`?

```java
// 1. ?? AgentCardProvider ?? Agent
A2aRemoteAgent remote = A2aRemoteAgent.builder()
        .name("data_analysis_agent")
        .agentCardProvider(agentCardProvider)  // ??? Nacos ?? AgentCard
        .description("????????")
        .build();

// 2. ????
Optional<OverAllState> result = remote.invoke("???????????????????");
```

## ????????

| ?? | ??? | ?? | ??????? |
|------|--------|------|----------------|
| **Registry????** | `registry.enabled: true` | ??? Agent ??? Nacos | ????? **?????** |
| **Discovery????** | `discovery.enabled: true` | ? Nacos ???? Agent | ????? **?????** |

??????????????
- ??**???**??? `data_analysis_agent` ? Nacos
- ??**???**????????????? Agent??????

## ????

1. **????**?
    - ???? `spring-ai-alibaba-starter-a2a-nacos` ??
    - ?? Nacos ??????

2. **Registry vs Discovery**?
    - `registry.enabled: true` - ???? Agent
    - `discovery.enabled: true` - ???? Agent
    - ??????????????

3. **? Agent ??**?
    - ?????????? Agent Bean ????
    - ?????? Agent???????????????????? Agent

4. **AgentCard ???**?
    - `server.card.name` ??? ReactAgent Bean ? `name` ??
    - `server.card.provider` ??????? Agent ?????

## ????

### Agent ????? Nacos
- ?? `registry.enabled: true` ????
- ????????? Nacos Registry AutoConfiguration ????
- ?? Nacos ?????server-addr?username?password?

### AgentCardProvider ???? Agent
- ?? `discovery.enabled: true` ????
- ?? Agent ?????? Nacos
- ?? agent name ????

### ??????
- ???? Agent ? REST API ?????
- ????????????
- ?? A2A ??????
